#include "day19.hpp"

namespace day19 {
    int part1(std::string input)
    {
        std::ifstream f;
        f.open(input);
        std::string line;
        while (std::getline(f, line))
        {

        }
        return 0;
    }
    int part2(std::string input)
    {
        std::ifstream f;
        f.open(input);
        std::string line;
        while (std::getline(f, line))
        {

        }
        return 0;
    }
}